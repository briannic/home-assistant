# Template for /opt/home-assistant/config/secrets.yaml on huron.
# The real secrets.yaml is git-ignored. Copy this file to secrets.yaml
# and fill in real values pulled from 1Password.
#
# Keys referenced from YAML via `!secret <name>`.

# Pushover — same credentials used by rip-dvd on chicago.
# 1Password item: Personal → "Pushover (Brian)"
pushover_user_key: REPLACE_FROM_1PASSWORD
pushover_api_token: REPLACE_FROM_1PASSWORD
